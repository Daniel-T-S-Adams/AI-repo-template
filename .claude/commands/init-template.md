# Compatibility: Initialize Template

The previous interactive Quick/Full initializer has been superseded by the repository's automatic intake workflow.

Use `docs/INTAKE.md` as the canonical procedure. This command remains only for compatibility with older habits and derived repositories.
