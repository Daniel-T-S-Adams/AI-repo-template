# Bootstrap

Use `docs/INTAKE.md` as the canonical first-agent procedure for a repository derived from `Daniel-T-S-Adams/AI-repo-template`.

In the source template itself, remain in source-template maintenance mode.
